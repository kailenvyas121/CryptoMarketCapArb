# Local setup

See the root [README](README.md) — `npm install && npm run dev` is the whole flow.

No API keys required. If port 5000 is busy on macOS (AirPlay Receiver), use:

```bash
PORT=5050 npm run dev
```
